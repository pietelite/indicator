def journeyPaperPluginExtension = extensions.getByName('journeyPaperPlugin')

jar {
    from '../LICENSE.txt'
    archiveBaseName.set(journeyPaperPluginExtension.pluginName.get() as String)
}

//sourceSets {
//    main.java.srcDirs += "${project.rootDir}/command/build/generated-src/antlr/main"
//}

processResources {
    outputs.upToDateWhen {
        false
    }
    def props = [version: version]
    inputs.properties props
    filteringCharset 'UTF-8'
    filesMatching('plugin.yml') {
        expand props
    }
}
